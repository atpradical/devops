.DS_Store
node_modules
test-results
.idea/


# committed: .env.development.* (local docker creds only)
# ignored: anything that might hold real secrets
.env
.env.local
.env.production
.env.*.local

# hw6: реальные Kubernetes-секреты и kubeconfig не коммитим.
# В git лежат только *.example.yaml-шаблоны из k8s/.
k8s/secret.yaml
k8s/dockerhub-secret.yaml
kubeconfig
*.kubeconfig

# hw8: приватный age-ключ для SOPS — никогда не коммитить
age-key.txt

# terraform
terraform/.terraform/
terraform/terraform.tfstate
terraform/terraform.tfstate.backup

# argocd keys
argocd/argocd-deploy-key

*.md

# SOFT: включается флагом monitoring.enabled (по умолчанию false). Требует CRD
# monitoring.coreos.com из kube-prometheus-stack — иначе ArgoCD-синк уйдёт в Degraded.
{{- if .Values.monitoring.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: backend-monitor
  namespace: {{ .Values.namespace }}
  labels:
    # Должен совпадать с именем Helm-release kube-prometheus-stack.
    release: {{ .Values.monitoring.release }}
spec:
  selector:
    matchLabels:
      app: {{ .Values.backend.name }}
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
{{- end }}
